import java.awt.*;
import javax.swing.*;

public class CenterPanelBuilder {
    public JPanel build() {
        JPanel panel = new JPanel(null);
        panel.setBackground(Color.WHITE);
        return panel;
    }
}
